import SwiftUI

extension ShortcutsTab {
    // MARK: - 业务逻辑（占位）

    func startRecording(id: String) {
        print("startRecording: \(id)")
    }

    func saveShortcut(id: String, keys: String) {
        print("saveShortcut: \(id) -> \(keys)")
    }

    func resetAllShortcuts() {
        print("resetAllShortcuts called")
    }
}
